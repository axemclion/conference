(function($, Backbone, app) {
	"use strict";
	app.View = app.View || {};

	app.View.Speakers = Backbone.View.extend({
		initialize: function() {
		}
	});
}(jQuery, Backbone, window.app = window.app || {}));